# Doc + wireframe audit report — Aventuras

All six parallel subagents returned. Findings are consolidated below — overlapping flags from multiple subagents are merged into single entries with combined evidence (the duplication itself confirms confidence). Lint baseline (`pnpm lint:docs`) passes — no broken anchors, no remark violations. Everything below is semantic / structural / drift.

This file is a **working ledger**: as findings are resolved, their entries are deleted (commit message carries the narrative). The whole file is removed once everything below is closed out.

## Summary

**Findings per class** (consolidated; counts update as entries are struck):

| Class                                              | Count |
| -------------------------------------------------- | ----- |
| Contradictions (principles vs per-screen)          | 2     |
| Wireframe vs doc drift                             | 7     |
| Stale / mis-targeted cross-references              | 0     |
| Duplicate prose across docs                        | 0     |
| Pattern coverage / asymmetry                       | 0     |
| Schema vs UI drift                                 | 0     |
| Architecture vs UI drift                           | 0     |
| Followups hygiene (resolved-not-removed / leakage) | 0     |
| Hand-waved decisions in canonical scope            | 2     |
| Naming / structural violations                     | 3     |
| Scope leakage (principles ↔ patterns ↔ followups)  | 5     |
| Heading-stability risks                            | 2     |

All five originally-flagged Top-5 issues are now resolved
(commits b06fc1c / 1690cc8 / 6c91fcc / f7e9949 / Batch C series).
Remaining work is in the per-class findings below.

---

## Findings

### Contradictions — principles vs per-screen

#### [Contradictions] "Set as lead" entry-point — principles claims World Panel · Overview, world.md places it in detail-head ⋯

- **Location(s)**: `docs/ui/principles.md:497-498` vs `docs/ui/screens/world/world.md:78-83`
- **What's wrong**: principles claims "Set as lead" on Overview "alongside status," but world.md places it in the detail-head ⋯ overflow menu rendered tab-agnostically (alongside Export / Delete / View raw JSON).
- **Evidence**:
  - principles.md:497 — `**World Panel · Overview** — "Set as lead" action on any character entity, alongside status.`
  - world.md:79 — `Set as lead (sets stories.settings.leadEntityId)` listed inside the `## Detail head structure` overflow menu (line 76: `An overflow menu (⋯) anchored to the name row`).
- **Suggested resolution**: Reconcile in one direction. Detail-head ⋯ home is consistent with world.md's other "rare-but-important" actions; the principle should say "World Panel · detail-head ⋯ menu". If Overview is the intended home, world.md needs updating + Overview should be promoted to its own subheading so principles can deep-link.
- **Confidence**: high

#### [Contradictions] "Set as lead" entry-point on Story Settings · About is fictional

- **Location(s)**: `docs/ui/principles.md:504` vs `docs/ui/screens/story-settings/story-settings.md:86-96`
- **What's wrong**: principles lists "Story Settings · About — alongside the mode selector" as one of four redundant lead entry points. About tab does NOT contain a mode selector (mode lives on Generation tab, line 92). Story-settings.md never mentions a "Set as lead" affordance on About OR Generation.
- **Evidence**:
  - principles.md:504 — `**Story Settings · About** — alongside the mode selector.`
  - story-settings.md:86-91 — About tab fields: title/description/genre/tags/author notes/cover/accent/library status/pin. No mode, no lead.
  - story-settings.md:92-96 — Generation tab carries mode + lead character.
- **Suggested resolution**: Either principles.md:504 should say `Story Settings · Generation` (matching where mode actually lives) AND story-settings.md should explicitly call out a "Set as lead" affordance, OR the entry-point is dropped from the redundancy list.
- **Confidence**: high

---

### Wireframe vs doc drift

#### [State coverage gap] Branch navigator wireframe doesn't demonstrate the "during generation" paused state

- **Location(s)**: `branch-navigator.md:114-145` vs `branch-navigator.html:135-151`
- **What's wrong**: Doc specifies a distinct in-flight state: rows at reduced opacity, row-body click non-interactive, top banner reading `"Generation in progress — switching paused."`, `×` icons hidden across all rows. HTML's review-controls bar has no toggle for this state, no banner element, no opacity rule.
- **Suggested resolution**: Add `gen-state` review control + banner + opacity rules.
- **Confidence**: high

#### [State coverage gap] Story Settings wireframe doesn't demonstrate the Definitional-change confirmation modal

- **Location(s)**: `story-settings.md:434-509` vs `story-settings.html:140-160`
- **What's wrong**: Doc dedicates a whole section to a save-time confirmation modal that intercepts dirty-session commits when `mode` / `narration` / `composerWrapPov` / `activePackId` are dirty AND the story has at least one entry. HTML wireframe has no modal element and no review-control toggle for "story has entries" / "story empty."
- **Suggested resolution**: Add a `definitional-confirm` review-control + modal element with the spec'd copy.
- **Confidence**: high

#### [State coverage gap] Chapter timeline doesn't demonstrate the "Close chapter… disabled during generation" state

- **Location(s)**: `chapter-timeline.md:366-368` vs `chapter-timeline.html:373-376`
- **What's wrong**: Doc explicitly says `"During the reader's pipeline phases ... the Close chapter… button on this screen should disable. Same rule as branch-navigator → during generation."` HTML topbar shows the gen-status pill in its visible state with no toggle for idle/in-flight; the `Close chapter…` button is fully active regardless.
- **Suggested resolution**: Add gen-state review control; disable Close-chapter + card-edit save bar during in-flight.
- **Confidence**: high

#### [State coverage gap] Reader composer never demonstrates the single-branch-hidden case

- **Location(s)**: `reader-composer.md:139-147` vs `reader-composer.html:462`
- **What's wrong**: Doc says `"Branch chip ... shown only when > 1 branch exists — single-branch stories omit it"`. HTML always renders the chip with badge `3`; no review-control to flip into the single-branch hidden state. Single-branch is the default for new stories.
- **Suggested resolution**: Add `branch-count` review-control (1 / 3).
- **Confidence**: medium

#### [State coverage gap] Plot panel doesn't demonstrate empty list-pane state

- **Location(s)**: `plot.md:281-283` vs `plot.html`
- **What's wrong**: Doc's open-questions explicitly says `"Empty states ... Drawn in v1 wireframe."` HTML lacks empty-state handling and toggle.
- **Suggested resolution**: Add `empty` review toggle, OR move the commitment from the doc to followups if deferral is intentional.
- **Confidence**: medium

#### [Wireframe drift] Onboarding step-2 footer link copy weak vs spec'd "real wizard exit"

- **Location(s)**: `onboarding.md:143-152` vs `onboarding.html:228-230`
- **What's wrong**: Doc says `"The footer link is a real wizard exit, not a passive hint. Clicking it ... routes to App Settings · Providers with the + Add provider ▾ type picker open"`. HTML renders plain `<a href="#">Other providers (Anthropic, OpenAI, Google) available in Settings →</a>` with no event handler beyond global `e.preventDefault()`. Reads as passive informational hint.
- **Suggested resolution**: Rewrite the HTML link copy or treatment to communicate the exit semantics, OR wire the click to the `setStep`/skip flow.
- **Confidence**: medium

---

### Hand-waved decisions in canonical scope

#### [TBD in canonical] chapter-timeline.md "Visual treatment TBD with the visual-identity pass"

- **Location(s)**: `chapter-timeline.md:367-368`
- **Suggested resolution**: Acceptable as a screen-specific open question; link to a generic visual-identity-pass followup if one exists.
- **Confidence**: low

#### [TBD in canonical] app-settings Diagnostics observability boundaries TBD

- **Location(s)**: `app-settings.md:446-449`
- **Suggested resolution**: Cross-link to observability followup if one exists.
- **Confidence**: low

---

### Naming / structural violations

#### [README prose violation] `docs/README.md` carries substantive prose despite its own "README is index only" rule

- **Location(s)**: `docs/README.md:25-166`
- **What's wrong**: Lines 110-114 declare README is "navigation only. No substantive content." The same file then carries Structure rules (canonical normative content!), Tooling, Common pitfalls, For Claude — all substantive prose. Self-violating.
- **Suggested resolution**: Either (a) carve out an explicit exception for the project root README which holds the structure rules, or (b) move "Structure rules" / "Tooling" / "Common pitfalls" / "For Claude" into a sibling canonical doc (e.g., `docs/conventions.md`) and reduce README.md to an index. (b) is rule-respecting; (a) is the smaller change but creates a "do as I say not as I do" precedent.
- **Confidence**: high

#### [README prose violation] `docs/calendar-systems/README.md` Tolkien canon note is substantive prose

- **Location(s)**: `docs/calendar-systems/README.md:23-29`
- **What's wrong**: Multi-sentence "Canon note" inside an index bullet.
- **Suggested resolution**: Reduce to one-liner; move explanation to a dedicated doc (e.g., `presets/README.md` or alongside the JSON).
- **Confidence**: medium

#### [README prose violation] `docs/ui/patterns/README.md` "When to add to this directory" is policy

- **Location(s)**: `docs/ui/patterns/README.md:42-47`
- **Suggested resolution**: Hoist criterion to `docs/README.md → Structure rules → Principles + patterns` (rule already lives there in spirit; this would tighten it).
- **Confidence**: medium

---

### Scope leakage (principles ↔ patterns ↔ followups)

#### [Scope leakage] `principles.md → Wireframe format and interactivity` is meta-tooling, not UI principle

- **Location(s)**: `principles.md:17-37`; partial duplicate in `.claude/rules/docs.md:42-49`
- **What's wrong**: This entire section describes how wireframe HTML files are authored (review-controls bar, monochrome, vanilla JS, no build) — doc-tooling guidance, not UI design philosophy.
- **Suggested resolution**: Move to `docs/README.md` under a "Wireframe authoring conventions" section, or extend the existing wireframe note in `.claude/rules/docs.md`.
- **Confidence**: high

#### [Scope leakage] Banner mutual-exclusion + priority rule lives in onboarding.md but applies to story-list

- **Location(s)**: `onboarding.md:313-323` (rule defined here); `story-list.md:222-224` (banner rendered here)
- **Suggested resolution**: Promote to principles.md (or patterns/banners.md) since the rule applies on a screen onboarding doesn't host.
- **Confidence**: medium

#### [Scope leakage] `patterns/calendar-picker.md → Implementation notes` mixes spec + open questions

- **Location(s)**: `patterns/calendar-picker.md:315-331`
- **What's wrong**: Six bullets, half forward-looking deferrals (search threshold, Select extension, tier-path tooltip), half real specs (Select extension, mobile bottom-sheet, keyboard accessibility).
- **Suggested resolution**: Fold real specs into main body; route deferrals to followups.md.
- **Confidence**: medium

#### [Scope leakage] `patterns/save-sessions.md → Why explicit save` is philosophy

- **Location(s)**: `patterns/save-sessions.md:16-22`
- **What's wrong**: Rationale section is philosophical / architecture-shaped — the kind of "why" content README says belongs in principles.
- **Suggested resolution**: Borderline. Acceptable to leave inline. Alternative: move to `principles.md → Edit restrictions during in-flight generation` as a sibling subsection.
- **Confidence**: low

#### [Scope leakage] Reader's "three-level entity surfacing" rephrased in two per-screen docs

- **Location(s)**: `reader-composer.md:18-19` (`Reader provides level 1 — Browse rail — and level 2 — peek drawer`); `world.md:8` (`The deep-edit workshop level of the three-level entity surfacing`)
- **Suggested resolution**: Either let `patterns/entity.md#entity-surfacing--three-levels-same-data` carry a single sentence mapping each level to its host surface, or accept these as helpful local context.
- **Confidence**: low

---

### Heading-stability risks

#### [Heading risk] principles.md "World Panel · Overview" linkbait phrase isn't a real heading anywhere

- **Location(s)**: `principles.md:497`
- **What's wrong**: principles names "World Panel · Overview" as a lead entry-point; world.md:88 uses `## Tabs (character kind)` and lists Overview as a sub-bullet. No anchor to deep-link to.
- **Suggested resolution**: If Overview becomes the actual home for "Set as lead" (post-resolution of the contradiction above), promote Overview to its own subheading in world.md so principles can deep-link.
- **Confidence**: medium

#### [Heading risk] `### Reader-only chrome` H3 deep-linked from per-screen

- **Location(s)**: `principles.md:132` (H3 inside `## Top-bar design rule`); `branch-navigator.md:271-272` cites `principles.md#reader-only-chrome`
- **Suggested resolution**: Informational. If/when "Top-bar design rule" gets restructured, grep for inbound `#reader-only-chrome`, `#universal-essentials`, `#universal-in-story-chrome`, `#settings-icon-scope`, `#master-detail-sub-header` before renaming.
- **Confidence**: low

---

## Cross-cutting observations

These are systemic patterns that recur across multiple findings — fixing the pattern fixes many instances.

1. **The principles/patterns split has a long tail.** When patterns/ was extracted from principles.md, URLs were updated but labels weren't (16 sites still say `[principles → X]`). Beyond labels, several patterns docs' "Used by" lists are out-of-date relative to what cites them, and several pattern-shaped sections still live in principles (Wireframe format, status pill click-to-cancel detail, banner contracts). One mass label-rename pass + a "Used by" audit + 1-2 promotion candidates would close most of the tail.

2. **Per-screen docs heavily restate cross-cutting boilerplate.** `## Top-bar`, `## Save session`, `## Detail pane — raw JSON viewer`, per-row import menu prose — the same paragraph appears in 3-5 places each. This is the failure mode the principles/patterns split was supposed to prevent. A single consolidation pass collapsing each per-screen restatement to "use the standard pattern + screen-specific deviations only" could shed substantial mass and drift risk.

3. **Wireframes lag spec on edge states.** Three high-stakes states are documented but not demonstrated: Branch navigator paused-during-generation; Story Settings definitional-change confirmation modal; Chapter Timeline disabled-during-generation. Each is a load-bearing edge case that gates a dangerous action. Reviewers can't see them today.

---

## What looks healthy

The audit isn't a hatchet job — substantial surfaces are clean and consistent.

- **Mechanical anchor integrity is perfect.** `pnpm lint:docs` passes; remark-validate-links resolves all ~370 anchors. No lint-config gap to file.
- **Followups format hygiene is upheld.** No `[done]` annotations, no strikethroughs, no "RESOLVED" tags. The outstanding-only invariant holds; the issues are content (resolved entries that should have been deleted), not format.
- **Bracketed inline text is clean.** No `[A|B]` / `[Foo ‖ Bar]` instances outside backticks or fenced code (which would otherwise tickle remark's reference-link parser).
- **Wireframe ↔ doc alignment is strong on most surfaces.** Onboarding, Plot panel, Rollback-confirm, World panel, the reader composer's main flow all align well — chrome, row indicators, modals, in-pane sub-headers, accordion grouping, save bars all match. The drift is concentrated in Story Settings (Memory tab segment, definitional-confirm modal) and Story-list (Import + banner + help-icon).
- **Cross-doc bidirectional citations are mostly symmetric.** `architecture.md ↔ ui/principles.md` Edit-restrictions cross-citations are bidirectional; `data-model.md → Aventuras file format` ↔ `patterns/data.md → Import counterparts` are consistent; `calendar-systems/spec.md → Resolves in followups.md` matches followups.md content (notwithstanding the resolved-not-removed finding).
- **Pattern doc structure (`patterns/README.md` + 7 pattern files) is solid.** Each pattern has a clear scope and a "Used by" list (even if some are out-of-date). The structural choice to split principles from patterns is paying off — the issue is keeping the boundary clean as new content lands, not the boundary itself.
- **Wireframe basename = doc basename rule is upheld.** Every per-screen doc has a matching `.html` colocated in the same directory. The single exception (`spec.md` next to `poc.html`) is a different artifact class (proof-of-concept, not wireframe) and is correctly named.
- **Composer modes / narration / mode / lead orthogonal-axes story is consistent.** Multiple per-screen docs handle the four-axis encoding (`mode` × `lead` × `narration` × `composerWrapPov`) without conflating any. Hard to do given the conceptual proximity; the docs do it.
- **Genuine open followups are still genuine.** Manual worldTime correction (cascade vs jump), top-K-by-salience long-term memory, granular per-story model controls, FTS5 search upgrade, image generation, encryption-at-rest — all remain real parked decisions, correctly scoped, and shouldn't be touched in any consolidation pass.
