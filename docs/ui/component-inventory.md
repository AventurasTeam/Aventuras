# Component inventory

Build queue for the remaining UI primitives, app-domain compound
components, and layout shells. Tracks: what's shipped, what's specced
and ready to scaffold, what needs a design pass before building, and
what's deferred.

Sister to [`components.md`](./components.md) (construction
conventions), [`patterns/README.md`](./patterns/README.md) (pattern
specs), and [`../followups.md`](../followups.md) (decision-shaped
open items). Rows move between states rather than leave: a
component that ships trades its build-ready row for a shipped one.

## States

- **shipped** — component file + story exist in the folder dictated
  by [components.md → Directory layout](./components.md#directory-layout).
- **needs-revision** — shipped, but the canonical contract evolved
  post-ship; code work is queued against the next implementation
  pass that touches the primitive (typically driven by a consumer
  needing the new capability). Entry leaves when the revision
  lands and the component re-enters `shipped` compliance.
- **build-ready** — pattern / foundation / per-screen doc fully
  specs the contract; no further design pass needed before
  scaffolding.
- **needs-design** — surface uses it or wireframe shows it, but
  there's no written contract yet. Run a design pass via
  [`aventuras-design`](../../.agents/skills/aventuras-design) before
  scaffolding.

## Primitives

Generic, single-purpose, reusable. Live in `components/ui/`.

### Primitives — shipped

Accordion, AlertDialog, Autocomplete, Avatar, Button, Checkbox,
Chip, ColorPicker, Dialog, EmptyState, Heading, Icon, IconAction, InlineEditableName,
Input, JSONBlock, MultiSelect, Popover, SearchableOverlayList, Select, Sheet,
Skeleton, Spinner, Stepper, Switch, SwitchVisual, Tabs, Tag, Textarea, Text,
Toast. Plus the `NativeOnlyAnimatedView` and `DisabledReasonTooltip` utility
wrappers.

### Primitives — needs revision

_Empty — every shipped primitive matches its spec._

### Primitives — build-ready

_Empty — every build-ready primitive has shipped._

### Primitives — needs design

_Empty — all primitives have specs._ As new primitive needs surface,
add rows here with the baseline column naming the react-native-reusables
source per [components.md → Sourcing](./components.md#sourcing--react-native-reusables-as-baseline)
(or `_none_` for from-scratch).

### Primitives — deferred

_Empty — no primitives currently blocked on a design decision._

Virtualization library choice is resolved (`@tanstack/react-virtual`
on web, `FlatList` on native) and validated inside Autocomplete's
suggestion list. A standalone `VirtualList` primitive isn't built:
the candidate second consumer — the reader narrative — resolved to
no virtualization at all (fully rendered flow inside the
[reader document](./patterns/reader-document.md)), so a shared
primitive waits for a consumer that actually needs one.

## Compounds

Peer compositions of primitives. Domain-agnostic and
multi-domain compounds live in `components/compounds/`;
single-domain compounds live in `components/<domain>/`. Folder
column distinguishes per-row. See
[components.md → Directory layout](./components.md#directory-layout)
for the rule.

### Compounds — shipped

| Compound                    | Folder                       | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------------------- | ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ActionsMenu                 | `components/compounds/`      | Universal command menu — a hybrid two-zone surface (curated global core and a screen-specific contextual zone) over a per-tier open surface (Popover desktop / Sheet phone). Trigger is the `⚲` top-bar icon plus `Cmd/Ctrl-K`. Composes `SearchableOverlayList`; combobox/listbox semantics, not a `role="menu"`. Spec: [actions-menu.md](./patterns/actions-menu.md).                                                                                                                                                                                                                                                                                                                                                                              |
| AppActionsMenu              | `components/compounds/`      | App-level Actions menu for the M1 chrome screens, split connected/pure: the connected `AppActionsMenu` reads the diagnostics gate (selector) and owns the Diagnostics-Hub navigation, so screens pass only their focus state as `hotkeyEnabled`; the presentational `AppActionsMenuPure` (props in) carries the Storybook gating/activation tests. Builds the `APP` core group with the Diagnostics-Hub entry gated on `diagnostics.enabled` (absent, not disabled, when off). Spec: [actions-menu.md](./patterns/actions-menu.md), [app-settings.md → Diagnostics](./screens/app-settings/app-settings.md#app--diagnostics).                                                                                                                        |
| AwarenessEditor             | `components/plot/`           | Inline per-row `useFieldArray` editor over `happening_awareness`: a character-only `EntityPicker`, an `EntryRefPicker` for `learned_at_entry_id`, `DecayResistanceField`, free-form `source`, per-row `Open in World` and remove. Swapped for `CommonKnowledgeNotice` while `common_knowledge` is on. Spec: [plot.md → Happenings side](./screens/plot/plot.md#happenings-side).                                                                                                                                                                                                                                                                                                                                                                     |
| Breadcrumb                  | `components/compounds/`      | Path segments separated by `/`; parent segments are links, the current segment is inert and emphasized ([`principles.md → Breadcrumb tappability`](./principles.md#breadcrumb-tappability)). Of the parents only the first (the story title) shrinks; the current segment caps at 70% and is a `TruncatedText`, so once truncated a tap reveals its full label; `size` is `sm` or `base`. World's top-bar title and master-detail sub-header; Story Settings' title converts in [Slice 4.4](../implementation/milestones/04-world-plot-read-surfaces/slices/04-story-settings-basic.md).                                                                                                                                                             |
| CalendarPicker              | `components/compounds/`      | Tier-adapted Select dropdown (rich two-line rows + `Manage in Vault →` tail) paired with an always-visible summary panel. Hosts: App Settings / Story Settings / Wizard. Summary-panel placement varies per host (adjacent / below). Swap-warning AlertDialog (W1 / W2 / W3) intentionally out of scope — ships as a separate composition layer.                                                                                                                                                                                                                                                                                                                                                                                                     |
| CollisionListRow            | `components/compounds/`      | Wraps shipped `ListRow` with a warn-tinted below-row collision strip carrying a `⚠ Collides with <other-name>` link and a `Resolve →` button. Strip is a sibling element outside the row's tap surface — own tap targets for link + button. World list-renderer's only call site at v1; pan-domain by contract so any future row-conditional warn extension can reuse the shape. Spec: [collision-resolve.md](./patterns/collision-resolve.md).                                                                                                                                                                                                                                                                                                      |
| CollisionResolveDialog      | `components/compounds/`      | Two-side collision resolution modal (Merge / Rename / Keep as distinct) for entity rows carrying `name_collision_flag`. Three body modes inline; merge body owns canonical pick + per-field radio + tag-deselect chips + relations summary; rename + keep bodies stateless or near-stateless. Pure View; Promise-driven `onResolve` driver wired by World consumer. Spec: [collision-resolve.md](./patterns/collision-resolve.md).                                                                                                                                                                                                                                                                                                                   |
| CollisionReviewPill         | `components/world/`          | The World top-bar `⚠ N need review` pill beside the generation pill: `Tag tone="warning"`, hidden at zero, glyph + count on phone, click reveals the first flagged row. Spec: [world.md → Surfacing](./screens/world/world.md#surfacing).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| CommonKnowledgeNotice       | `components/plot/`           | `EmptyState` swap for the happening pane's Awareness tab body while `common_knowledge` is on — no add affordance, matching the schema's common-knowledge-skips-awareness invariant. Spec: [plot.md → Happenings side](./screens/plot/plot.md#happenings-side).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| DecayResistanceField        | `components/plot/`           | `low / medium / high` preset `Chip` row (0.2 / 0.5 / 0.8) over a fractional `NumberInput`; a chip reads selected only on an exact match, so a continuous classifier-written value displays with none lit. Spec: [plot.md → Happenings side](./screens/plot/plot.md#happenings-side).                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| DeltaLogRow                 | `components/compounds/`      | History-tab row across World, Plot, and the future global delta-log surface. Renders one `deltas` entry: op, target, field path, pre-rendered diff summary, source. Spec: [delta-log-row.md](./patterns/delta-log-row.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| DetailTabs                  | `components/compounds/`      | Picks Tab strip vs the `Select` primitive per [tabs.md → Tab-strip overflow rule](./patterns/tabs.md#tab-strip-overflow-rule) — Plot's thread pane strips on desktop/tablet and Select-segments on phone, its happening pane strips on desktop only and Select-dropdowns on tablet/phone; renders inside the consumer's `<Tabs>` Root, filling `DetailPane`'s `tabs` slot. Spec: [plot.md → Mobile expression](./screens/plot/plot.md#mobile-expression).                                                                                                                                                                                                                                                                                            |
| DiagnosticsSettingsPanel    | `components/compounds/`      | The App Settings · Diagnostics tab body — master "Enable diagnostics capture" `SwitchRow` over a "Include debug-level emissions" `SwitchRow` that disables when master is off. Presentational (booleans + handlers in); the route binds the handlers to the settings action layer. Spec: [app-settings.md → Diagnostics](./screens/app-settings/app-settings.md#app--diagnostics).                                                                                                                                                                                                                                                                                                                                                                   |
| EmbedderDownloadDialog      | `components/compounds/`      | Three-payload install workflow modal (curated catalog / HF id / custom file import). Single canonical component invoked from Onboarding Step 4, App Settings · Embedding models · Add model, and Story Settings · Memory · Switch embedder. Pure View + reducer-driven container; ships with a stub driver. Real platform drivers wired per consumer. Spec: [embedder-download.md](./patterns/embedder-download.md).                                                                                                                                                                                                                                                                                                                                 |
| EmbedWindowCounter          | `components/compounds/`      | Reserved row reporting how close a text is to the embedder's input window — quiet under 75%, warn-tinted past 100%. `document` variant for stored text (a lore body or entity description, whose tail becomes permanently unretrievable) and `query` for a user action (which searches memory on its prefix and is re-asked next turn). Always render it: gating on the pressure remounts a sibling `TextInput` mid-entry per [input-adornment-dom-identity](../implementation/lessons-learned/input-adornment-dom-identity.md). Hosts: Composer, and `EmbedWindowTextarea`.                                                                                                                                                                         |
| EmbedWindowTextarea         | `components/compounds/`      | `Textarea` paired with `EmbedWindowCounter`, owning the wrapper so the always-mounted rule holds without each consumer composing it correctly. Counts through the local model's own tokenizer (exact) or an estimator for a provider (marked approximate). Hosts: Wizard lore body, Wizard cast description. Spec: [retrieval.md → What gets embedded per type](../memory/retrieval.md#what-gets-embedded-per-type).                                                                                                                                                                                                                                                                                                                                 |
| EntityDetailPane            | `components/world/detail/`   | C7 host for the four entity kinds: switches on `kind` to `CharacterDetailPane`, `LocationDetailPane`, `ItemDetailPane` or `FactionDetailPane`, each a `useEntityRowSession` form inside the shared `EntityDetailFrame` (inline-editable name, `Recently classified` badge, the `⋯` menu with `Set as lead` on characters and the raw-JSON viewer, `DetailTabs`, the row save bar and leave dialog). Tabs: Overview (`EntityOverview`), Identity, Carrying (characters only), Connections, Settings, an Assets placeholder, read-only Involvements rows that open the happening in Plot, and a History placeholder. A null `row` is create mode. Spec: [world.md → Tabs — per-kind composition](./screens/world/world.md#tabs--per-kind-composition). |
| EntityKindIcon              | `components/entity/`         | 22×22 box, Lucide glyph centered (User / MapPin / Package / Flag, and ScrollText for lore), named with the translated kind. Color inherits via `TextClassContext`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| EntityOverview              | `components/world/overview/` | Read-mostly glance card per kind, width-agnostic so 4.5b's peek can project it: status pill with the non-default injection-mode chip, description, the kind's regions (a character's visual line, traits, drives, location, faction and carrying summary; a location's parent chain and inverse counts; an item's condition and position; a faction's standing, agenda and members), tags, and an `Avatar` portrait placeholder that drops below the prose on `variant="peek"` and on phone. Region labels and bodies call `onRegionPress(tab)`; entity names are sibling links calling `onOpenEntity(id)`. Spec: [world.md → Overview](./screens/world/world.md#overview--glance-summary-read-mostly).                                              |
| EntityPicker                | `components/compounds/`      | Kind-aware `SearchableOverlayList` picker (in-overlay mode) over the branch's entities, returning an entity id since two rows can share a name. Sections group by kind, and a status `Tag` marks a non-active row; a `value` absent from `entities` (the link tables carry no FK) renders a missing-entity warning instead of the placeholder. Trigger is `PickerField`. An optional `rowHint(entity)` adds a muted note after a row's name (World's Carrying picker shows an item's whereabouts).                                                                                                                                                                                                                                                   |
| EntityRefList               | `components/world/detail/`   | The Carrying tab's `equipped_items` / `inventory` list: one removable row per item id (an unresolved id renders the missing-entity warning) over an item-only `EntityPicker` whose `rowHint` names each item's holder or place. `excludeIds` hides the other list's items from the picker; a per-list `addLabel` keeps the two add pickers distinctly named. Spec: [world.md → Carrying](./screens/world/world.md#carrying--character-only).                                                                                                                                                                                                                                                                                                         |
| EntityRow                   | `components/entity/`         | C2 row renderer over `ListRow` for the four entity kinds: kind glyph, lead badge, status pill, in-scene stripe, recently-classified tint, and the `CollisionListRow` strip when flagged. No description line; `density` is accepted for the rail but has no visible effect on entity rows today.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| EntryCard                   | `components/compounds/`      | Reader-composer narrative row. Renders all five entry kinds (`user`, `ai`, `opening`, `system`, `streaming`) as full-width bubbles with kind-keyed styling, conditional reasoning body, in-place edit, muted world-time footer. Spec: [entry-card.md](./patterns/entry-card.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| EntryRefPicker              | `components/compounds/`      | Sibling picker over the branch's entries, returning an entry id and rendering `entry #n` plus its excerpt through the shared `EntryRefText` sub-component, also used read-only elsewhere. Search parses a leading `#` as position-only, bare digits as position-or-excerpt, and anything else as an excerpt match; a `value` with no matching entry (rolled away) renders the dangling state instead of the placeholder. Trigger is `PickerField`.                                                                                                                                                                                                                                                                                                   |
| FormRow                     | `components/compounds/`      | Container-keyed stacked-vs-2-col layout (label / hint / error / required slot). Library-agnostic — caller pipes the error string.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| GenerationStatusPill        | `components/compounds/`      | Top-bar generation status pill (universal in-story chrome per [`principles.md → Universal in-story chrome`](./principles.md#universal-in-story-chrome)). Owns the priority machine (active generation > sticky error > hidden), maps phase + error enums to copy, renders tier-aware (icon-only on phone), and owns the click-to-cancel Popover for the active variant. Error variant taps fire `onErrorTap(code)` for the consumer to route. Spec: [generation-status-pill.md](./patterns/generation-status-pill.md).                                                                                                                                                                                                                               |
| HappeningDetailPane         | `components/plot/`           | C7 host for the happening row: Overview (title, description, category, icon, common-knowledge `SwitchRow`, mutually exclusive time anchor), Involvements and Awareness tabs, a History placeholder, the raw-JSON viewer merging the row's link rows, and the `⋯` menu. Spec: [plot.md → Happenings side](./screens/plot/plot.md#happenings-side).                                                                                                                                                                                                                                                                                                                                                                                                    |
| HappeningRow                | `components/plot/`           | C2 row renderer over `ListRow` for happenings: `PlotIcon`, title, category as the description line, a when-marker tag, and a common-knowledge ⊙ glyph slot that stays reserved rather than conditionally rendered, so every row keeps one layout.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ImportDialog                | `components/compounds/`      | Pure-View Dialog driving every `.avts` import: file pick / clipboard read, envelope meta-check, zod payload validation, hybrid meta-error vs payload-error display. Generic over `TPayload`; host wires the trigger (`ImporterMenu` for world / plot / vault-calendars; flat Button for story-list). Implementation prerequisite: `expo-document-picker` + `expo-file-system` installed; consuming slice must trigger a dev-client rebuild before native runtime. Spec: [import-dialog.md](./patterns/import-dialog.md).                                                                                                                                                                                                                             |
| ImporterMenu                | `components/compounds/`      | Trigger button + popover menu shell for `+ New X ▾` / `+ Add Y ▾` / `Import …` affordances. Renders only the menu chrome (label, options, disabled-reason); per-option actions are caller-supplied. Icon trigger (`trigger="icon"`) with the label as a hover tooltip, and controlled `open` / `onOpenChange` for the list panes.                                                                                                                                                                                                                                                                                                                                                                                                                    |
| InvolvementsEditor          | `components/plot/`           | Inline per-row `useFieldArray` editor over `happening_involvements`: a kind-aware `EntityPicker` over all four kinds plus free-form `role`, per-row `Open in World` and remove. Spec: [plot.md → Happenings side](./screens/plot/plot.md#happenings-side).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| JSONViewer                  | `components/compounds/`      | Tier-adapted Sheet (right drawer ≥ tablet, bottom sheet on phone) carrying `Raw JSON · <name>` header + Copy + ×, monospace pretty-printed body, disabled `Edit raw — coming later` footer. Read-only in v1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ListRow                     | `components/compounds/`      | Pan-domain row carrying four indicator channels: lead-badge (slot), status-pill (slot), scene-presence (typed left-edge stripe), recently-classified (typed background-tint enum).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| LoreRow                     | `components/entity/`         | C2 row renderer over `ListRow` for lore: `EntityKindIcon`'s lore glyph, title, body excerpt, category tag, recently-classified tint; `density="compact"` drops the excerpt.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ModuleList                  | `components/list/`           | The C2 module body inside the list-pane shell: a pinned row plus a tier accordion or a flat list, the collapsed-tier `⚠ N` badge, and reveal-and-scroll via `useRevealScroll`. Takes `collapsed` / `onCollapsedChange`; generic over the module's signals so Plot's pane (4.3) can reuse it.                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| NumberInput                 | `components/compounds/`      | Controlled numeric text field over `Input`, reporting `null` for empty or unparseable text (plain decimal digits only, so no hex or exponent forms; decimal mode also accepts a comma separator) so the owner refuses the save instead of clamping; integer by default, `integer={false}` for fractions. Width follows [forms.md → Input width within form rows](./patterns/forms.md#input-width-within-form-rows).                                                                                                                                                                                                                                                                                                                                  |
| OverflowMenu                | `components/compounds/`      | The detail-head `⋯`: an ordered list of entries (`key`, `label`, `disabled?`, `disabledReason?`, `destructive?`, `onPress`) behind a Popover trigger on desktop/tablet and a short, content-fit bottom Sheet on phone. A disabled entry's accessible name is `label, reason` (WCAG 2.5.3 — the visible label stays first), surfaced as a web tooltip / native inline hint. Shared contract between World's and Plot's detail panes.                                                                                                                                                                                                                                                                                                                  |
| PickerField                 | `components/compounds/`      | The Input-sized trigger `EntityPicker` and `EntryRefPicker` share: placeholder or committed-value children, optional clear `×`, disabled-reason tooltip. Spreads the `SearchableOverlayList` trigger's asChild props and merges refs so the overlay anchors correctly on web.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| PlotIcon                    | `components/plot/`           | 22×22 glyph wrapper, the same box `EntityKindIcon` uses so Plot rows align with World's. Resolves `threads.icon` / `happenings.icon` preset-catalog keys to a Lucide glyph, falling back per kind (`Diamond` for threads, `Zap` for happenings) when the stored key doesn't match a catalog entry.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| PlotListPane                | `components/plot/`           | `EntityListPane` composed over the C2 thread and happening modules through `ModuleList`: the Threads/Happenings segment `Select`, per-side filter chips, status-tier or chapter-bucket accordion, empty and no-results states; `revealRow` widens a hiding chip or search and expands the row's group for a deep link. Spec: [plot.md → Implementation reuse](./screens/plot/plot.md#implementation-reuse).                                                                                                                                                                                                                                                                                                                                          |
| ProviderModelPicker         | `components/compounds/`      | Searchable, grouped, rich-row picker for `{providerId, modelId}` composite. Composes `SearchableOverlayList` in `'in-overlay'` mode; trigger button (modelId + capability icons + chevron, with provider-missing / model-not-in-catalog warning variants), cross-provider Favorites pinned section, grouped provider sections with sticky headers, sticky-footer custom-add composer. Spec: [provider-model-picker.md](./patterns/provider-model-picker.md).                                                                                                                                                                                                                                                                                         |
| RelationshipsEditor         | `components/world/detail/`   | The character Connections tab's Relationships sub-section: a `ListRow` per pair describing both views, a row tap expanding an inline edit card (a character-only `EntityPicker` excluding self and already-related characters, your-view and their-view inputs, Delete, Done), and a pinned `+ Add relationship` row. Inline rather than a Sheet because the phone picker is itself one; cards key by `cardKey`, so a session reset doesn't collapse an open card. Spec: [world.md → Relationships](./screens/world/world.md#relationships--character-to-character).                                                                                                                                                                                 |
| RowSaveBar / RowLeaveDialog | `components/compounds/`      | The save-bar and leave-dialog chrome over a `useRowSaveSession` (`hooks/use-row-save-session.ts`): `RowSaveBar` wraps `SaveBar`, mounted only while the session is dirty; `RowLeaveDialog` wraps `UnsavedChangesDialog` for a queued navigate-away. Both take an optional `blocked` / `blockedReason` gate layered over the session's own `invalidReason`.                                                                                                                                                                                                                                                                                                                                                                                           |
| SaveBar                     | `components/compounds/`      | Save-session footer — dirty-note left, Discard / Save right. Bg derived from `--warning` at 12% via overlay-View (cross-platform). `⌘/Ctrl-S` shortcut on web only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| StackablesEditor            | `components/world/detail/`   | The Carrying tab's quantities as editable rows (key `Input`, count `NumberInput`, remove) with an `Add quantity` button; rows key by index so a session reset doesn't remount the row being typed in, and a duplicate key revalidates every row. Spec: [world.md → Carrying](./screens/world/world.md#carrying--character-only).                                                                                                                                                                                                                                                                                                                                                                                                                     |
| StoryCard                   | `components/story/`          | Story List grid card. Single consumer; visual shape pinned in [story-list.md](./screens/story-list/story-list.md#story-card--text-first). Typed compound contract plus favorite-star visibility deviation from icon-actions. Spec: [story-card.md](./patterns/story-card.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| StoryStatusPill             | `components/compounds/`      | Connected leaf over `GenerationStatusPill` that every in-story top bar renders: reads the story's memory health (`hooks/use-memory-health.ts`) and raises it as the pill's error, so the stale count's per-batch ticks re-render the pill and not the screen. Every error it raises routes to Story Settings · Memory, so consumers pass `onOpenMemory` in place of `onErrorTap`. Spec: [generation-status-pill.md](./patterns/generation-status-pill.md).                                                                                                                                                                                                                                                                                           |
| SuggestionCategoriesEditor  | `components/compounds/`      | Per-category editor list for next-turn suggestion categories. Per-row drag handle (dnd-kit sortable on web; native uses a custom reanimated + gesture-handler drag rendering plain Views — no virtualization, so the editor nests inside any ScrollView without warnings), enable toggle, label input (non-empty + case-insensitive unique with inline errors), `ColorPicker` swatch, prompt-hint textarea, delete. Phone collapses rows into an `Accordion`. Used by Story Settings → Generation tab (Suggestion categories) and App Settings → Story Defaults. Spec: [story-settings.md → Suggestion categories](./screens/story-settings/story-settings.md#suggestion-categories).                                                                |
| SwitchRow                   | `components/compounds/`      | Label + description + toggle, three-peer row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| TagInput                    | `components/compounds/`      | Multi-value, iterative tag entry. Composes Tag + Input primitives into a bordered surface accumulating committed tags as removable chips alongside inline typing. Spec: [forms.md → TagInput pattern](./patterns/forms.md#taginput-pattern).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ThreadDetailPane            | `components/plot/`           | C7 host for the thread row: Overview (status, category, icon, description, `injection_mode`, the two read-only entry refs), a History placeholder, the raw-JSON viewer, and the `⋯` menu. Spec: [plot.md → Threads side](./screens/plot/plot.md#threads-side).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ThreadRow                   | `components/plot/`           | C2 row renderer over `ListRow` for threads: `PlotIcon`, title, a category tag dropped at compact density, and a status pill toned by `THREAD_STATUS_TONE`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Toolbar                     | `components/compounds/`      | List-pane chrome compound: search, filter chips, conditional sort, cross-tier overflow rule that wraps cleanly from desktop single-row to narrow stacked-rows. Spec: [toolbar.md](./patterns/toolbar.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| TruncatedText               | `components/compounds/`      | One line of inert chrome text that, only while it truncates, becomes a button showing the full string in a `Popover`, dismissed on tap-outside or after a 4 s idle. Web reads the text node's own overflow; native compares against a hidden unconstrained copy. `containerClassName` is the box and the tap target, so a touch floor goes there. Hosts: the reader and Story Settings top-bar titles, `Breadcrumb`'s current segment. Spec: [touch.md → Tap-to-tooltip on inert chrome text](./foundations/mobile/touch.md#tap-to-tooltip-on-inert-chrome-text).                                                                                                                                                                                    |
| UnsavedChangesDialog        | `components/compounds/`      | Save / Discard / Cancel `AlertDialog` for a leave guarded mid-edit; moved here from Story Settings so `RowLeaveDialog` and other save-session hosts share one component. Disables Save while `saveDisabled`, shows the blocking reason under the body, and ignores Escape / back while `saving` so a commit in flight can't resolve as cancel.                                                                                                                                                                                                                                                                                                                                                                                                       |
| WorldDetailPlaceholder      | `components/world/`          | `DetailPane` host for World's no-selection state and Lore rows until 4.2b: `Select a row` empty state, or the lore title and recently-classified badge over a lands-in body. No tabs, no `⋯` menu.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| WorldListPane               | `components/world/`          | `EntityListPane` composed over the C2 modules: category `Select`, filter chips, in-memory query, tier accordion with the collapsed `⚠ N` badge and the lead pinned above the tiers, empty and no-results states; `revealRow` handles the review pill and strip-link jump.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

### Compounds — build-ready

_Empty — every compound either ships or has a spec awaiting design._

### Compounds — needs design

_Empty — every compound either ships or has a spec._

## Layout shells

Top-level layout primitives.

### Shells — shipped

| Shell              | Folder               | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------ | -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DetailPane         | `components/shells/` | Master detail pane for World + Plot. Head (`nameSlot` + badges + ⋯) over a horizontal separator, then a `Tabs` strip and a scrollable body, with an optional `SaveBar` slot pinned to the bottom. `tabs` and `overflowMenu` are required; a read-only placeholder passes `null`. Consumer wraps the pane in a single `<Tabs>` Root and splits strip / bodies across the `tabs` + `children` slots so the shell can visually anchor the strip below the head.                                                                                                                                                                                                          |
| EntityListPane     | `components/shells/` | Master list pane for World + Plot. Kind-selector row carries an `addSlot` for the `[+]` affordance (an `IconAction` or an `ImporterMenu` with `trigger="icon"`). Composes `Toolbar` for search / filter chips / optional sort; switches to a consumer-rendered `emptyState` slot when `isEmpty`. Row rendering and the kind-selector ReactNode are consumer territory.                                                                                                                                                                                                                                                                                                |
| MasterDetailLayout | `components/shells/` | Tier-aware 2-pane shell for World + Plot. tablet / desktop: side-by-side with `listPaneWidth` (default 340 px) + flex-1 detail and a vertical divider. phone (< 640 px via `useTier()`): list-first — `isRowSelected` toggles which pane is visible while both stay mounted so internal state (scroll, composer draft, dirty save-session) survives every transition per `collapse.md → State preservation`. Optional `subHeader` slot above the panes per `principles.md → Master-detail sub-header`. Spec: [collapse.md](./foundations/mobile/collapse.md), [layout.md](./foundations/mobile/layout.md), [principles.md](./principles.md#master-detail-sub-header). |
| ScreenShell        | `components/shells/` | Chrome wrapper — variant-driven (`app-root` / `app` / `in-story`). Owns left slot (Logo vs ← Return), variant-derived right cluster, token-progress strip (always rendered on `in-story` for layout stability), banner stack, and tier-aware reshuffle of reader chip cluster via `useTier()`. Page fills slots for title / status / extras.                                                                                                                                                                                                                                                                                                                          |

### Shells — build-ready

_Empty — every build-ready shell has shipped._

### Shells — needs design

_Empty — every shell question is resolved. Reader-composer's
chrome stays all-locality (no extracted shell); revisited at the
reader-composer build pass only if cross-surface signal surfaces._
