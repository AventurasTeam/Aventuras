/**
 * Compatibility Shims
 *
 * Computes deprecated pre-formatted variables (tieredContextBlock, chapterSummaries, etc.)
 * for templates that still reference old variable names.
 *
 * Shim logic is added in Phase 6. This file is created in Phase 1 as the
 * designated home per COMPAT-05.
 *
 * @see REQUIREMENTS.md COMPAT-01..05
 */

// Phase 6 will add: computeShims(context, templateSource) -> Record<string, string>
export {}
